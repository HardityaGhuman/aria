# Minimal monochrome dark theme — black, white, neutral grays only.
[theme]
base = "dark"
primaryColor = "#FAFAFA"
backgroundColor = "#0A0A0A"
secondaryBackgroundColor = "#151515"
textColor = "#EDEDED"
font = "sans serif"

[client]
toolbarMode = "minimal"
